This repository contains an Alteryx workflow (real-estate-linear-regression-alteryx.yxmd) built to model and predict housing prices using final-real-estate.csv. The pipeline processes the dataset, performs an association analysis to check feature correlations, and splits the data into an 80/20 train-test ratio. It then trains a standardized multiple linear regression model, tests it against the validation data, and outputs the absolute prediction error.
